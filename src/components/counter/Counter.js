import React from 'react';

function Counter({number}) {
    const quant = new Array(1)

    return (
        quant.map(item => {
            return <h1>,jhkjh</h1>
        })
    )
}

export default Counter;